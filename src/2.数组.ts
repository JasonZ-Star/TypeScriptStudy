//数组类型
// 第一种定义方式：直接定义
let arr1: number[] = [1, 2, 3]
arr1.forEach(number => {
    console.log(number)
})

// 第二种定义方式：泛型定义
let arr2: Array<number> = [4, 5, 6]
arr2.forEach(number => {
    console.timeLog()
})

